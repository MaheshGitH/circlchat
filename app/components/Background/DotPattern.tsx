import React from "react";
import styles from "./styles.module.css";

const DotPattern = () => {
  return (
    <div
      className={`${styles.dotPattern} lg:size-60 absolute self-center`}
    ></div>
  );
};

export default DotPattern;
